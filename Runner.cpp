#include "Runner.hpp"

namespace Dualys::exec
{

    Result<Exit> run(const Policy &)
    {
        // Implémentation de la fonction run
    }

    Result<Handle> spawn(const Policy &)
    {
        // Implémentation de la fonction spawn
    }

}
